---
spring:
  application:
    name: msoauthserver-microservice
  cloud:  
    config:
      uri: http://localhost:8080

server:
  port: 8009
      
#Define Security

security:
  basic:
    enabled: false
  user:
    password: password
    
  ignored:
    - /css/**,/js/**,favicon.ico,/webjars/**
